[package]
name = "harnel-notebook"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
publish = false

# Deliberately independent: this application resolves Harnel from crates.io.
[workspace]

[dependencies]
harnel = "0.1.0"
tokio = { version = "1", features = ["macros", "rt-multi-thread", "signal"] }
